const REPO="aliciamiacheng/job-application-tracker",FILE="applications.csv";
function parseCSV(text){const rows=[];let row=[],cell="",q=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(q&&c==='"'&&n==='"'){cell+='"';i++;}else if(c==='"')q=!q;else if(c===","&&!q){row.push(cell);cell="";}else if((c==="\n"||c==="\r")&&!q){if(c==="\r"&&n==="\n")i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell="";}else cell+=c;}if(cell||row.length){row.push(cell);rows.push(row)}if(!rows.length)return[];const h=rows[0];return rows.slice(1).map(r=>Object.fromEntries(h.map((k,i)=>[k,r[i]||""])));}
export default async function handler(req,res){if(req.method!=="GET")return res.status(405).json({error:"GET only"});if(!process.env.GITHUB_TOKEN)return res.status(500).json({error:"Missing server configuration"});const r=await fetch(`https://api.github.com/repos/${REPO}/contents/${FILE}`,{headers:{Authorization:"Bearer "+process.env.GITHUB_TOKEN,Accept:"application/vnd.github+json","X-GitHub-Api-Version":"2022-11-28"}});if(!r.ok)return res.status(502).json({error:"Could not read tracker"});const j=await r.json(),raw=Buffer.from(j.content.replace(/\n/g,""),"base64").toString("utf8");res.setHeader("Cache-Control","no-store");return res.status(200).json({applications:parseCSV(raw)});}
